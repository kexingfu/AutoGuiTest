#移动操作图片识别准确度, 此设置为默认值, 调用方法时仍可指定
moveConfidence = 0.9

#点击操作图片识别准确度, 此设置为默认值, 调用方法时仍可指定
clickConfidence = 0.9

#移动操作鼠标移动时间, 此设置为默认值, 调用方法时仍可指定
moveDuration = 0

#点击操作鼠标移动时间, 此设置为默认值, 调用方法时仍可指定
clickDuration = 0

#拖拽时间, 此设置为默认值, 调用方法时仍可指定
dragDuration = 0.3

#灰度匹配
grayscale = False

#默认图片格式, 此设置为默认值, 调用方法时仍可指定
imageType = 'png'

#log输出等级('info' 或 'warning')
logRank = 'info'

#查找图片多少次后不再查找，直接退出或跳过此步继续执行, 此设置为默认值, 调用方法时仍可指定
maxloop = 999

#查找图片多少次后仍未找到，直接退出(True) 还是 跳过此步继续执行(False) ,此设置为默认值, 调用方法时仍可指定
maxloopExit = True